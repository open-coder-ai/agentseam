# Antigravity

> Generated by `examples/generate.py`. Do not edit by hand.

Every block below is produced by running agentseam, so it is what this agent
actually gets -- not an illustration of what it might get.

## What agentseam claims about it

| | |
|---|---|
| tier | `block` |
| enforcement at `pre_tool` | **best-effort** |
| can block | yes |
| can rewrite | no |
| hook config | `.agents/hooks.json` |
| verified | 2026-08-26 (vendor hooks documentation read directly (per-event schemas and decision vocabulary)) |

Richest decision vocabulary here -- allow, deny, ask, force_ask and deny_unless_prior_grant -- but no rewrite, since permissionOverrides widens permissions rather than changing arguments. The payload carries NO event name, so the event is inferred from shape, and PreToolUse/PostToolUse differ only by an `error` field documented as empty rather than absent; ties go to PreToolUse because the opposite guess would skip the gate. PreInvocation and PostInvocation have identical payloads and are unmapped. Fail mode is not documented, so the weaker claim (open) is recorded rather than a guess in our own favour.

## 1. What `agentseam install` writes

Wiring one handler for the pre-tool gate. agentseam owns only the entries it adds;
an uninstall removes exactly those and leaves anything else in the file alone.

`.agents/hooks.json`

```json
{
  "agentseam": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ]
  }
}
```

## 2. The event the handler sees

The same situation for every agent: **a secret about to be written to a file.**

This agent sends:

```json
{
  "conversationId": "example",
  "modelName": "gemini-3.6-flash-medium",
  "stepIdx": 3,
  "toolCall": {
    "args": {
      "CodeContent": "AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-KEY",
      "TargetFile": "AGENTS.md"
    },
    "name": "write_to_file"
  },
  "workspacePaths": [
    "/repo"
  ]
}
```

which agentseam normalizes to:

```
event    = pre_tool
tool     = write_to_file
path     = AGENTS.md
command  = None
content  = AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-
```

## 3. What each decision looks like coming back

### `Decision.allow()` -- the handler is happy

```json
{"decision": "allow"}
```

Exit code: `0`

### `Decision.deny()` -- the handler refuses

```json
{"decision": "deny", "reason": "secret detected in file content"}
```

Exit code: `0`

### `Decision.ask()` -- the handler wants a human

```json
{"decision": "ask", "reason": "looks like a credential -- confirm?"}
```

Exit code: `0`

### `Decision.rewrite()` -- the handler wants the input changed

> reduced to `ask` by the dispatcher: this agent cannot rewrite

```json
{"decision": "deny", "reason": "redacting the secret (Antigravity cannot modify a tool call)"}
```

Exit code: `0`

## 4. Other primitives

**Permissions** -- no model recorded: a permission system provably exists -- its hooks return permissionOverrides, its decisions respect a user's "Always Allow", and it exposes list_permissions and ask_permission as tools -- but the config schema behind it was not read here


**Packaging** -- no format recorded: subagents provably exist -- define_subagent and invoke_subagent are tools it offers, and its file reader takes an IsSkillFile argument -- but the on-disk layout was not established here

