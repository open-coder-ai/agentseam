# Claude Code

> Generated by `examples/generate.py`. Do not edit by hand.

> **How this was established.** live-run: observed against a running agent. Checked 2026-08-25.
>
> Vendors change their hook surfaces without telling us. Confirm against your own
> installation before relying on any of it, and open an issue if a page is wrong --
> a claim that has quietly stopped being true is the failure mode this project
> cares most about.


One section per hook this agent is claimed to support, in lifecycle order. Every
block is produced by running agentseam, so it is what this agent actually gets.

## What agentseam claims

| | |
|---|---|
| tier | `block+rewrite` |
| enforcement at `pre_tool` | **enforced** |
| can block / rewrite | yes / yes |
| hooks covered | 12 |
| hook config | `.claude/settings.json` |
| evidence | `live-run` — live headless run + official hooks reference |

Richest surface (~30 events). Blocks via exit 2 or hookSpecificOutput.permissionDecision; rewrite via updatedInput (pre_tool only). Live capture 2026-08-27 found detection broken against current builds: Claude Code now sends prompt_id, which this adapter treated as proof a payload was Devin's, so 38 of 42 real payloads were claimed by Devin instead and a deny rendered in the wrong dialect. Fixed by discriminating on fields observed from Claude Code rather than on a field it was assumed to lack.

## What `agentseam install` writes

One handler wired for every hook this agent supports.

`.claude/settings.json`

```json
{
  "hooks": {
    "FileChanged": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "InstructionsLoaded": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ]
  }
}
```

## Every hook, one at a time

The story is held constant across agents so the pages compare: the same credential
heading for a memory file, the same failing test, the same prompt.

### 1. `session_start` — called `SessionStart` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SessionStart",
  "permission_mode": "default",
  "session_id": "example",
  "source": "startup",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = session_start
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "SessionStart", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 2. `session_end` — called `SessionEnd` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SessionEnd",
  "permission_mode": "default",
  "reason": "exit",
  "session_id": "example",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = session_end
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "SessionEnd", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 3. `prompt_submit` — called `UserPromptSubmit` here

Enforcement: **enforced**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "UserPromptSubmit",
  "permission_mode": "default",
  "prompt": "remember my aws key so you can deploy later",
  "session_id": "example",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = prompt_submit
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "UserPromptSubmit", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 4. `pre_tool` — called `PreToolUse` here

Enforcement: **enforced**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "PreToolUse",
  "permission_mode": "default",
  "session_id": "example",
  "tool_input": {
    "content": "AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-KEY",
    "file_path": "AGENTS.md"
  },
  "tool_name": "Write",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = pre_tool
tool     = Write
path     = AGENTS.md
content  = AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-
```

This is the gate, so every decision is worth seeing.

**`Decision.allow()`** — the handler is happy

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "allow"}}
```

Exit code: `0`

**`Decision.deny()`** — the handler refuses

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "secret detected in file content"}}
```

Exit code: `0`

**`Decision.ask()`** — the handler wants a human

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "ask", "permissionDecisionReason": "looks like a credential -- confirm?"}}
```

Exit code: `0`

**`Decision.rewrite()`** — the handler wants the input changed

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "allow", "updatedInput": {"content": "AWS_SECRET_ACCESS_KEY=<redacted>"}, "permissionDecisionReason": "redacting the secret"}}
```

Exit code: `0`

### 5. `post_tool` — called `PostToolUse` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "PostToolUse",
  "permission_mode": "default",
  "session_id": "example",
  "tool_input": {
    "command": "npm test"
  },
  "tool_name": "Bash",
  "tool_output": "ok",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = post_tool
tool     = Bash
command  = npm test
output   = ok
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PostToolUse", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 6. `tool_failure` — called `PostToolUseFailure` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "error": "1 test failed",
  "hook_event_name": "PostToolUseFailure",
  "permission_mode": "default",
  "session_id": "example",
  "tool_input": {
    "command": "npm test"
  },
  "tool_name": "Bash",
  "tool_output": "1 test failed",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = tool_failure
tool     = Bash
command  = npm test
output   = 1 test failed
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PostToolUseFailure", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 7. `pre_compact` — called `PreCompact` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "PreCompact",
  "permission_mode": "default",
  "session_id": "example",
  "transcript_path": "/repo/.claude/transcript.jsonl",
  "trigger": "auto"
}
```

which normalizes to:

```
event    = pre_compact
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PreCompact", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 8. `stop` — called `Stop` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "Stop",
  "permission_mode": "default",
  "session_id": "example",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = stop
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "Stop", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 9. `subagent_start` — called `SubagentStart` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SubagentStart",
  "permission_mode": "default",
  "session_id": "example",
  "subagent_type": "explore",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = subagent_start
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "SubagentStart", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 10. `subagent_stop` — called `SubagentStop` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SubagentStop",
  "permission_mode": "default",
  "session_id": "example",
  "subagent_type": "explore",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = subagent_stop
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "SubagentStop", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 11. `instructions_loaded` — called `InstructionsLoaded` here

Enforcement: **detect**.

The agent sends:

```json
{
  "content": "Prefer pnpm. Tests live beside source.",
  "cwd": "/repo",
  "file_path": "CLAUDE.md",
  "hook_event_name": "InstructionsLoaded",
  "permission_mode": "default",
  "session_id": "example",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = instructions_loaded
path     = CLAUDE.md
content  = Prefer pnpm. Tests live beside source.
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "InstructionsLoaded", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 12. `file_changed` — called `FileChanged` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "file_path": "AGENTS.md",
  "hook_event_name": "FileChanged",
  "permission_mode": "default",
  "session_id": "example",
  "transcript_path": "/repo/.claude/transcript.jsonl"
}
```

which normalizes to:

```
event    = file_changed
path     = AGENTS.md
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "FileChanged", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

## Other primitives

**Permissions** — `deny shell curl *` rendered into `.claude/settings.json`:

```json
{
  "permissions": {
    "deny": [
      "Bash(curl *)"
    ]
  }
}
```

**Packaging** — a one-skill bundle, rooted at `.claude`:

```
.claude-plugin/plugin.json
skills/secret-scan/SKILL.md
```
