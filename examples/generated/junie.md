# Junie CLI

> Generated by `examples/generate.py`. Do not edit by hand.

> **How this was established.** vendor-docs: read from the vendor's documentation -- a claim about what the vendor says, not an observation of what their build does. Checked 2026-08-26.
>
> Vendors change their hook surfaces without telling us. Confirm against your own
> installation before relying on any of it, and open an issue if a page is wrong --
> a claim that has quietly stopped being true is the failure mode this project
> cares most about.


One section per hook this agent is claimed to support, in lifecycle order. Every
block is produced by running agentseam, so it is what this agent actually gets.

## What agentseam claims

| | |
|---|---|
| tier | `block+rewrite` |
| enforcement at `pre_tool` | **best-effort** |
| can block / rewrite | yes / yes |
| hooks covered | 5 |
| hook config | `~/.junie/config.json` |
| evidence | `vendor-docs` — vendor hooks documentation read directly (events, decisions, config merging, limitations) |

Strongest gate here: PreToolUse returns allow/ask/block and carries updatedInput, so block, ask and rewrite are all native. Its event names and field names are Claude Code's by design; project_path is what separates the two payloads. Project-local hooks in <project>/.junie/config.json are IGNORED by default for safety, so a guardrail committed to a repo does not run for a teammate who clones it -- the user file is the only location that takes effect without --config-location. PermissionRequest inverts the usual default: a hook exiting 0 without a blocking decision approves the action and skips the dialog the user would have seen. StopFailure is observability-only and fires on LLM/API failures, not tool failures, so it is not tool_failure and is left unmapped. Non-zero exits other than 2 are warnings and execution proceeds, so this fails OPEN.

## What `agentseam install` writes

One handler wired for every hook this agent supports.

`~/.junie/config.json`

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "type": "command"
          }
        ]
      }
    ]
  }
}
```

## Every hook, one at a time

The story is held constant across agents so the pages compare: the same credential
heading for a memory file, the same failing test, the same prompt.

### 1. `session_start` — called `SessionStart` here

Enforcement: **detect**.

> Also claimed by **gemini_cli**, so `detect()` declines this payload and the agent has
> to be named: `handle(raw, handler, agent="junie")`. Guessing between adapters
> that answer differently is worse than declining.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SessionStart",
  "project_path": "/repo",
  "session_id": "example",
  "source": "startup"
}
```

which normalizes to:

```
event    = session_start
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 2. `session_end` — called `SessionEnd` here

Enforcement: **detect**.

> Also claimed by **gemini_cli**, so `detect()` declines this payload and the agent has
> to be named: `handle(raw, handler, agent="junie")`. Guessing between adapters
> that answer differently is worse than declining.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SessionEnd",
  "project_path": "/repo",
  "reason": "exit",
  "session_id": "example"
}
```

which normalizes to:

```
event    = session_end
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 3. `prompt_submit` — called `UserPromptSubmit` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "UserPromptSubmit",
  "project_path": "/repo",
  "prompt": "remember my aws key so you can deploy later",
  "session_id": "example"
}
```

which normalizes to:

```
event    = prompt_submit
```

A `Decision.deny()` here produces:

```json
{"decision": "deny", "reason": "policy violation"}
```

Exit code: `0`

### 4. `pre_tool` — called `PreToolUse` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "PreToolUse",
  "project_path": "/repo",
  "session_id": "example",
  "tool_input": {
    "content": "AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-KEY",
    "file_path": "AGENTS.md"
  },
  "tool_name": "Write"
}
```

which normalizes to:

```
event    = pre_tool
tool     = Write
path     = AGENTS.md
content  = AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-
```

This is the gate, so every decision is worth seeing.

**`Decision.allow()`** — the handler is happy

```json
{"decision": "allow"}
```

Exit code: `0`

**`Decision.deny()`** — the handler refuses

```json
{"decision": "deny", "reason": "secret detected in file content"}
```

Exit code: `0`

**`Decision.ask()`** — the handler wants a human

```json
{"decision": "ask", "reason": "looks like a credential -- confirm?"}
```

Exit code: `0`

**`Decision.rewrite()`** — the handler wants the input changed

```json
{"decision": "allow", "updatedInput": {"content": "AWS_SECRET_ACCESS_KEY=<redacted>"}, "reason": "redacting the secret"}
```

Exit code: `0`

### 5. `stop` — called `Stop` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "Stop",
  "project_path": "/repo",
  "session_id": "example"
}
```

which normalizes to:

```
event    = stop
```

A `Decision.deny()` here produces:

```json
{"decision": "block", "reason": "policy violation"}
```

Exit code: `0`

## Other primitives

**Permissions** — no model recorded: a permission system provably exists -- PermissionRequest is one of its hook events, and it lists the tool categories the dialog covers -- but the settings that govern it were not read here


**Packaging** — no format recorded: extensions provably exist and ship hooks at hooks/hooks.json in the Claude plugin layout, with ${JUNIE_EXTENSION_ROOT} aliasing ${CLAUDE_PLUGIN_ROOT} -- but the rest of the extension layout, and how skills and commands sit in it, was not read here

