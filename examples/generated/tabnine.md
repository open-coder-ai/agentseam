# Tabnine CLI

> Generated by `examples/generate.py`. Do not edit by hand.

> **How this was established.** vendor-docs: read from the vendor's documentation -- a claim about what the vendor says, not an observation of what their build does. Checked 2026-08-26.
>
> Vendors change their hook surfaces without telling us. Confirm against your own
> installation before relying on any of it, and open an issue if a page is wrong --
> a claim that has quietly stopped being true is the failure mode this project
> cares most about.


One section per hook this agent is claimed to support, in lifecycle order. Every
block is produced by running agentseam, so it is what this agent actually gets.

## What agentseam claims

| | |
|---|---|
| tier | `block` |
| enforcement at `pre_tool` | **best-effort** |
| can block / rewrite | yes / no |
| hooks covered | 7 |
| hook config | `.tabnine/agent/settings.json` |
| evidence | `vendor-docs` — vendor hooks documentation read directly (event table, exit codes, output fields) |

Eleven events, six of which can block -- including AfterTool, which most agents treat as observation only. Event names are Gemini CLI's exactly; the only documented separator is `timestamp` in Tabnine's base schema, which identifies Tabnine but cannot exclude Gemini, so detect() declines these payloads and the agent must be named explicitly. Fails OPEN unusually broadly: any exit code other than 0 or 2 proceeds, AND stdout that is not valid JSON is treated as a systemMessage and the action is ALLOWED -- so a chatty or crashed hook is a permitted action. Rewrite is advertised in the vendor overview but the field carrying it was not in the documentation read here, so no rewrite is claimed.

## What `agentseam install` writes

One handler wired for every hook this agent supports.

`.tabnine/agent/settings.json`

```json
{
  "hooks": {
    "AfterAgent": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "name": "agentseam",
            "type": "command"
          }
        ]
      }
    ],
    "AfterTool": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "name": "agentseam",
            "type": "command"
          }
        ]
      }
    ],
    "BeforeAgent": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "name": "agentseam",
            "type": "command"
          }
        ]
      }
    ],
    "BeforeTool": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "name": "agentseam",
            "type": "command"
          }
        ]
      }
    ],
    "PreCompress": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "name": "agentseam",
            "type": "command"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "name": "agentseam",
            "type": "command"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "python3 guard.py",
            "name": "agentseam",
            "type": "command"
          }
        ]
      }
    ]
  }
}
```

## Every hook, one at a time

The story is held constant across agents so the pages compare: the same credential
heading for a memory file, the same failing test, the same prompt.

### 1. `session_start` — called `SessionStart` here

Enforcement: **detect**.

> Also claimed by **vscode_copilot**, so `detect()` declines this payload and the agent has
> to be named: `handle(raw, handler, agent="tabnine")`. Guessing between adapters
> that answer differently is worse than declining.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SessionStart",
  "session_id": "example",
  "source": "startup",
  "timestamp": "2026-08-26T00:00:00.000Z"
}
```

which normalizes to:

```
event    = session_start
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 2. `session_end` — called `SessionEnd` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "SessionEnd",
  "reason": "exit",
  "session_id": "example",
  "timestamp": "2026-08-26T00:00:00.000Z"
}
```

which normalizes to:

```
event    = session_end
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 3. `prompt_submit` — called `BeforeAgent` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "BeforeAgent",
  "prompt": "remember my aws key so you can deploy later",
  "session_id": "example",
  "timestamp": "2026-08-26T00:00:00.000Z"
}
```

which normalizes to:

```
event    = prompt_submit
```

A `Decision.deny()` here produces:

```json
{"decision": "deny", "reason": "policy violation"}
```

Exit code: `0`

### 4. `pre_tool` — called `BeforeTool` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "BeforeTool",
  "session_id": "example",
  "timestamp": "2026-08-26T00:00:00.000Z",
  "tool_input": {
    "content": "AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-KEY",
    "file_path": "AGENTS.md"
  },
  "tool_name": "Write"
}
```

which normalizes to:

```
event    = pre_tool
tool     = Write
path     = AGENTS.md
content  = AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-
```

This is the gate, so every decision is worth seeing.

**`Decision.allow()`** — the handler is happy

```json
{"decision": "allow"}
```

Exit code: `0`

**`Decision.deny()`** — the handler refuses

```json
{"decision": "deny", "reason": "secret detected in file content"}
```

Exit code: `0`

**`Decision.ask()`** — the handler wants a human

```json
{"decision": "deny", "reason": "looks like a credential -- confirm? (Tabnine cannot prompt for confirmation)"}
```

Exit code: `0`

**`Decision.rewrite()`** — the handler wants the input changed

> reduced to `ask`: this agent cannot rewrite

```json
{"decision": "deny", "reason": "redacting the secret (Tabnine cannot modify a tool call)"}
```

Exit code: `0`

**`Decision.vouch()`** — the handler actively approves

> reduced to `allow`: this agent cannot vouch

```json
{"decision": "allow"}
```

Exit code: `0`

### 5. `post_tool` — called `AfterTool` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "AfterTool",
  "session_id": "example",
  "timestamp": "2026-08-26T00:00:00.000Z",
  "tool_input": {
    "command": "npm test"
  },
  "tool_name": "Bash",
  "tool_output": "ok"
}
```

which normalizes to:

```
event    = post_tool
tool     = Bash
command  = npm test
output   = ok
```

A `Decision.deny()` here produces:

```json
{"decision": "deny", "reason": "policy violation"}
```

Exit code: `0`

### 6. `pre_compact` — called `PreCompress` here

Enforcement: **detect**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "PreCompress",
  "session_id": "example",
  "timestamp": "2026-08-26T00:00:00.000Z",
  "trigger": "auto"
}
```

which normalizes to:

```
event    = pre_compact
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 7. `stop` — called `AfterAgent` here

Enforcement: **best-effort**.

The agent sends:

```json
{
  "cwd": "/repo",
  "hook_event_name": "AfterAgent",
  "session_id": "example",
  "timestamp": "2026-08-26T00:00:00.000Z"
}
```

which normalizes to:

```
event    = stop
```

A `Decision.deny()` here produces:

```json
{"decision": "deny", "reason": "policy violation"}
```

Exit code: `0`

## Other primitives

**Permissions** — no model recorded: no permission model established here; vendor documentation is unreachable from this environment


**Packaging** — no format recorded: no packaging format established here; vendor documentation is unreachable from this environment

