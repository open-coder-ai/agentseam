# Cursor

> Generated by `examples/generate.py`. Do not edit by hand.

> **How this was established.** vendor-docs: read from the vendor's documentation -- a claim about what the vendor says, not an observation of what their build does. Checked 2026-08-26.
>
> Vendors change their hook surfaces without telling us. Confirm against your own
> installation before relying on any of it, and open an issue if a page is wrong --
> a claim that has quietly stopped being true is the failure mode this project
> cares most about.


One section per hook this agent is claimed to support, in lifecycle order. Every
block is produced by running agentseam, so it is what this agent actually gets.

## What agentseam claims

| | |
|---|---|
| tier | `block+rewrite` |
| enforcement at `pre_tool` | **enforceable** |
| can block / rewrite | yes / yes |
| hooks covered | 11 |
| hook config | `.cursor/hooks.json` |
| evidence | `vendor-docs` — vendor hooks documentation read directly (event list, per-event schemas, exit codes) |

Fails OPEN by default; failClosed:true per hook definition makes it fail closed, and agentseam sets it on every gate it installs. `ask` is accepted by the preToolUse schema but not enforced today, so the adapter denies instead of returning a prompt that would behave as a pass; beforeShellExecution and beforeMCPExecution do honour ask. Separate Tab hooks (beforeTabFileRead, afterTabFileEdit) gate inline completions, and workspaceOpen fires outside any session with no canonical event here. Cursor also loads Claude Code-format hooks -- confirmed live (2026-08-27): with both `.cursor/hooks.json` and `.claude/settings.json` carrying the same probe, every event fired twice, once per config. Witnessed live (Windows, 2026-08-27): a beforeShellExecution hook that produced no stdout caused the tool call to be REJECTED -- an empty response is not an allow, so silence blocks and the documented fail-open covers something narrower than any-hook-problem. A hook wired here must answer in-dialect.

## What `agentseam install` writes

One handler wired for every hook this agent supports.

`.cursor/hooks.json`

```json
{
  "hooks": {
    "afterFileEdit": [
      {
        "command": "python3 guard.py"
      }
    ],
    "beforeSubmitPrompt": [
      {
        "command": "python3 guard.py"
      }
    ],
    "postToolUse": [
      {
        "command": "python3 guard.py"
      }
    ],
    "postToolUseFailure": [
      {
        "command": "python3 guard.py"
      }
    ],
    "preCompact": [
      {
        "command": "python3 guard.py"
      }
    ],
    "preToolUse": [
      {
        "command": "python3 guard.py",
        "failClosed": true
      }
    ],
    "sessionEnd": [
      {
        "command": "python3 guard.py"
      }
    ],
    "sessionStart": [
      {
        "command": "python3 guard.py"
      }
    ],
    "stop": [
      {
        "command": "python3 guard.py"
      }
    ],
    "subagentStart": [
      {
        "command": "python3 guard.py"
      }
    ],
    "subagentStop": [
      {
        "command": "python3 guard.py"
      }
    ]
  },
  "version": 1
}
```

## Every hook, one at a time

The story is held constant across agents so the pages compare: the same credential
heading for a memory file, the same failing test, the same prompt.

### 1. `session_start` — called `sessionStart` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "sessionStart",
  "model": "claude-opus-4-7-thinking-max",
  "source": "startup",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = session_start
tool     = sessionStart
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 2. `session_end` — called `sessionEnd` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "sessionEnd",
  "model": "claude-opus-4-7-thinking-max",
  "reason": "exit",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = session_end
tool     = sessionEnd
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 3. `prompt_submit` — called `beforeSubmitPrompt` here

Enforcement: **enforceable**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "beforeSubmitPrompt",
  "model": "claude-opus-4-7-thinking-max",
  "prompt": "remember my aws key so you can deploy later",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = prompt_submit
tool     = beforeSubmitPrompt
```

A `Decision.deny()` here produces:

```json
{"continue": false, "user_message": "policy violation"}
```

Exit code: `0`

### 4. `pre_tool` — called `preToolUse` here

Enforcement: **enforceable**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "preToolUse",
  "model": "claude-opus-4-7-thinking-max",
  "tool_input": {
    "content": "AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-KEY",
    "file_path": "AGENTS.md"
  },
  "tool_name": "Write",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = pre_tool
tool     = Write
path     = AGENTS.md
content  = AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-
```

This is the gate, so every decision is worth seeing.

**`Decision.allow()`** — the handler is happy

```json
{"permission": "allow"}
```

Exit code: `0`

**`Decision.deny()`** — the handler refuses

```json
{"permission": "deny", "user_message": "secret detected in file content", "agent_message": "secret detected in file content"}
```

Exit code: `0`

**`Decision.ask()`** — the handler wants a human

```json
{"permission": "deny", "user_message": "looks like a credential -- confirm? (preToolUse cannot prompt for confirmation, so this is a block)", "agent_message": "looks like a credential -- confirm? (preToolUse cannot prompt for confirmation, so this is a block)"}
```

Exit code: `0`

**`Decision.rewrite()`** — the handler wants the input changed

```json
{"permission": "allow", "updated_input": {"content": "AWS_SECRET_ACCESS_KEY=<redacted>"}}
```

Exit code: `0`

### 5. `post_tool` — called `postToolUse` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "postToolUse",
  "model": "claude-opus-4-7-thinking-max",
  "tool_input": {
    "command": "npm test"
  },
  "tool_name": "Bash",
  "tool_output": "ok",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = post_tool
tool     = Bash
command  = npm test
output   = ok
```

A `Decision.deny()` here produces:

```json
{"additional_context": "observed after the fact (postToolUse cannot prevent it): policy violation"}
```

Exit code: `0`

### 6. `tool_failure` — called `postToolUseFailure` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "error": "1 test failed",
  "generation_id": "gen-1",
  "hook_event_name": "postToolUseFailure",
  "model": "claude-opus-4-7-thinking-max",
  "tool_input": {
    "command": "npm test"
  },
  "tool_name": "Bash",
  "tool_output": "1 test failed",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = tool_failure
tool     = Bash
command  = npm test
output   = 1 test failed
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 7. `pre_compact` — called `preCompact` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "preCompact",
  "model": "claude-opus-4-7-thinking-max",
  "trigger": "auto",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = pre_compact
tool     = preCompact
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 8. `stop` — called `stop` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "stop",
  "model": "claude-opus-4-7-thinking-max",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = stop
tool     = stop
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 9. `subagent_start` — called `subagentStart` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "subagentStart",
  "model": "claude-opus-4-7-thinking-max",
  "subagent_type": "explore",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = subagent_start
tool     = subagentStart
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 10. `subagent_stop` — called `subagentStop` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "generation_id": "gen-1",
  "hook_event_name": "subagentStop",
  "model": "claude-opus-4-7-thinking-max",
  "subagent_type": "explore",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = subagent_stop
tool     = subagentStop
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

### 11. `file_changed` — called `afterFileEdit` here

Enforcement: **detect**.

The agent sends:

```json
{
  "conversation_id": "example",
  "cursor_version": "1.7.2",
  "cwd": "/repo",
  "file_path": "AGENTS.md",
  "generation_id": "gen-1",
  "hook_event_name": "afterFileEdit",
  "model": "claude-opus-4-7-thinking-max",
  "workspace_roots": [
    "/repo"
  ]
}
```

which normalizes to:

```
event    = file_changed
tool     = afterFileEdit
path     = AGENTS.md
```

A `Decision.deny()` here produces:

_Nothing. This event is observation only, so a decision is not read._

Exit code: `0`

## Other primitives

**Permissions** — no model recorded: its hook surface is now recorded (see the matrix), and hooks are a permission mechanism -- but the settings-file allow/deny model is a separate thing, and that documentation was not available here


**Packaging** — no format recorded: plugins exist -- hooks can be installed through them, and workspaceOpen returns pluginPaths for the workspace -- but the directory layout was not established here, so there is nothing to render

