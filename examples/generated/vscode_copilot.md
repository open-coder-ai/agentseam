# GitHub Copilot (VS Code agent mode / CLI)

> Generated by `examples/generate.py`. Do not edit by hand.

> **How this was established.** vendor-source: read from the vendor's own source code. Checked 2026-08-26.
>
> Vendors change their hook surfaces without telling us. Confirm against your own
> installation before relying on any of it, and open an issue if a page is wrong --
> a claim that has quietly stopped being true is the failure mode this project
> cares most about.


One section per hook this agent is claimed to support, in lifecycle order. Every
block is produced by running agentseam, so it is what this agent actually gets.

## What agentseam claims

| | |
|---|---|
| tier | `block+rewrite` |
| enforcement at `pre_tool` | **enforced** |
| can block / rewrite | yes / yes |
| hooks covered | 6 |
| hook config | `.github/hooks/*.json` |
| evidence | `vendor-source` — microsoft/vscode source: languageModelToolsService.invokeTool + hookCommandTypes |

Same PreToolUse contract as Claude Code (permissionDecision/updatedInput) and parses Claude settings.json via hookClaudeCompat. Memory writes arrive as the 'memory' tool (create/str_replace/insert), not file edits.

## What `agentseam install` writes

One handler wired for every hook this agent supports.

`.github/hooks/agentseam.json`

```json
{
  "hooks": [
    {
      "command": "python3 guard.py",
      "event": "sessionStart"
    },
    {
      "command": "python3 guard.py",
      "event": "sessionEnd"
    },
    {
      "command": "python3 guard.py",
      "event": "userPromptSubmitted"
    },
    {
      "command": "python3 guard.py",
      "event": "preToolUse"
    },
    {
      "command": "python3 guard.py",
      "event": "postToolUse"
    },
    {
      "command": "python3 guard.py",
      "event": "postToolUseFailure"
    }
  ],
  "version": 1
}
```

## Every hook, one at a time

The story is held constant across agents so the pages compare: the same credential
heading for a memory file, the same failing test, the same prompt.

### 1. `session_start` — called `sessionStart` here

Enforcement: **detect**.

The agent sends:

```json
{
  "hookEventName": "sessionStart",
  "session_id": "example"
}
```

which normalizes to:

```
event    = session_start
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 2. `session_end` — called `sessionEnd` here

Enforcement: **detect**.

The agent sends:

```json
{
  "hookEventName": "sessionEnd",
  "session_id": "example"
}
```

which normalizes to:

```
event    = session_end
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 3. `prompt_submit` — called `userPromptSubmitted` here

Enforcement: **detect**.

The agent sends:

```json
{
  "hookEventName": "userPromptSubmitted",
  "prompt": "remember my aws key so you can deploy later",
  "session_id": "example"
}
```

which normalizes to:

```
event    = prompt_submit
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 4. `pre_tool` — called `preToolUse` here

Enforcement: **enforced**.

The agent sends:

```json
{
  "hookEventName": "preToolUse",
  "session_id": "example",
  "tool_input": {
    "command": "create",
    "file_text": "AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-KEY",
    "path": "/memories/team.md"
  },
  "tool_name": "memory"
}
```

which normalizes to:

```
event    = pre_tool
tool     = memory
path     = /memories/team.md
content  = AWS_SECRET_ACCESS_KEY=EXAMPLE-PLACEHOLDER-NOT-A-
```

This is the gate, so every decision is worth seeing.

**`Decision.allow()`** — the handler is happy

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "allow"}}
```

Exit code: `0`

**`Decision.deny()`** — the handler refuses

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "secret detected in file content"}}
```

Exit code: `0`

**`Decision.ask()`** — the handler wants a human

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "ask", "permissionDecisionReason": "looks like a credential -- confirm?"}}
```

Exit code: `0`

**`Decision.rewrite()`** — the handler wants the input changed

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "allow", "updatedInput": {"content": "AWS_SECRET_ACCESS_KEY=<redacted>"}}}
```

Exit code: `0`

### 5. `post_tool` — called `postToolUse` here

Enforcement: **detect**.

The agent sends:

```json
{
  "hookEventName": "postToolUse",
  "session_id": "example",
  "tool_input": {
    "command": "npm test"
  },
  "tool_name": "runInTerminal",
  "tool_output": "ok"
}
```

which normalizes to:

```
event    = post_tool
tool     = runInTerminal
command  = npm test
output   = ok
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

### 6. `tool_failure` — called `postToolUseFailure` here

Enforcement: **detect**.

The agent sends:

```json
{
  "hookEventName": "postToolUseFailure",
  "session_id": "example",
  "tool_input": {
    "command": "npm test"
  },
  "tool_name": "runInTerminal",
  "tool_output": "1 test failed"
}
```

which normalizes to:

```
event    = tool_failure
tool     = runInTerminal
command  = npm test
output   = 1 test failed
```

A `Decision.deny()` here produces:

```json
{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "policy violation"}}
```

Exit code: `0`

## Other primitives

**Permissions** — `deny shell curl *` rendered into `.vscode/settings.json`:

```json
{}
```

> Not representable here: this map has no deny: setting a pattern false withholds auto-approval but still lets a human approve the command, so it would not block


**Packaging** — a one-skill bundle, rooted at `.`:

```
.github/skills/secret-scan/SKILL.md
```
