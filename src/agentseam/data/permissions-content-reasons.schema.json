{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/open-coder-ai/agentseam/blob/main/src/agentseam/data/permissions-content-reasons.schema.json",
  "title": "agentseam content-rule reasons",
  "description": [
    "Why each recorded permission surface cannot express a ContentRule: none of them reads",
    "the bytes a tool touches, only names and command/path patterns. One entry per agent in",
    "data/permissions-capability.json; the note is spoken verbatim as the Unrepresentable",
    "reason, carrying basis/date only where its source states one."
  ],
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "additionalProperties": false,
    "required": ["note"],
    "properties": {
      "note": {"type": "string", "minLength": 1},
      "basis": {"enum": ["live-run", "live-run-partial", "vendor-source", "vendor-docs", "third-party-install", "inherited"]},
      "date": {"type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"}
    }
  }
}
